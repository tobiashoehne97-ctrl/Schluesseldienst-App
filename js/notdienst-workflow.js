/* Klarer Notdienst-Einsatzablauf + Zeitdokumentation
   Zeit ist Dokumentation und beeinflusst niemals automatisch den Preis.
*/
(function(){
  const KEY='schluesseldienst-notdienst-zeiten-v3';
  const pad=n=>String(n).padStart(2,'0');
  const now=()=>new Date().toISOString();
  const read=(k,d)=>{try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch(e){return d}};
  const write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const fmt=t=>t?new Date(t).toLocaleTimeString('de-DE',{hour:'2-digit',minute:'2-digit'}):'–';
  const duration=(a,b)=>a&&b?Math.max(0,new Date(b)-new Date(a)):0;
  const durationText=ms=>{const m=Math.floor(ms/60000),h=Math.floor(m/60),r=m%60;return `${h} Std. ${pad(r)} Min.`};
  const getTimes=id=>read(KEY,{})[id]||{};
  const setTime=(id,key)=>{const all=read(KEY,{});all[id]=all[id]||{};if(!all[id][key])all[id][key]=now();write(KEY,all);return all[id]};
  let activeId=null,timer=null;
  function entry(id){return (window.AppData?.kalender?.eintraege||[]).find(e=>String(e.id)===String(id));}
  function address(e){return [[e.strasse,e.hausnummer].filter(Boolean).join(' '),[e.postleitzahl,e.ort].filter(Boolean).join(' ')].filter(Boolean).join(', ')}
  function ensureStyle(){if(document.getElementById('ndWfStyle'))return;const s=document.createElement('style');s.id='ndWfStyle';s.textContent=`#ndWorkflowModal{position:fixed;inset:0;z-index:99999;background:rgba(3,12,24,.84);backdrop-filter:blur(5px);display:none;align-items:center;justify-content:center;padding:18px}#ndWorkflowModal.open{display:flex}.nd-wf{width:min(680px,100%);max-height:calc(100vh - 36px);overflow:auto;background:#102c43;border:1px solid #315a7c;border-radius:24px;box-shadow:0 24px 70px rgba(0,0,0,.5);color:#eaf5ff;padding:24px}.nd-wf-top{display:flex;justify-content:space-between;align-items:flex-start;gap:16px}.nd-wf-top h2{margin:4px 0;font-size:25px}.nd-wf-sub{color:#8fb8d8;font-size:14px}.nd-wf-close{border:1px solid #315a7c;background:#183a54;color:#dcefff;border-radius:12px;padding:9px 12px;font-size:18px}.nd-wf-status{display:inline-block;padding:7px 11px;border-radius:999px;background:#194e78;color:#bfe2ff;font-weight:800;font-size:12px;text-transform:uppercase}.nd-wf-clock{text-align:center;padding:30px 10px 24px}.nd-wf-clock .time{font-size:58px;font-weight:800}.nd-wf-clock .label{color:#8fb8d8;font-size:14px}.nd-wf-info{background:#163850;border:1px solid #285675;border-radius:16px;padding:15px;margin-bottom:18px}.nd-wf-info strong{display:block;font-size:17px;margin-bottom:5px}.nd-wf-info span{display:block;color:#a8c7df;font-size:14px;line-height:1.5}.nd-wf-btn{width:100%;border:0;border-radius:15px;padding:16px;font-size:17px;font-weight:800;cursor:pointer;margin-top:10px}.nd-wf-success{background:#22a06b;color:white}.nd-wf-primary{background:#287dcc;color:white}.nd-wf-steps{display:flex;gap:7px;margin:18px 0}.nd-wf-step{flex:1;height:5px;border-radius:4px;background:#25445b}.nd-wf-step.active{background:#3787c8}.nd-wf-step.done{background:#2caf75}.nd-wf-summary{display:grid;grid-template-columns:1fr 1fr;gap:9px;margin:16px 0}.nd-wf-summary div{background:#163850;border:1px solid #285675;border-radius:12px;padding:11px}.nd-wf-summary small{display:block;color:#80a9c9;font-size:11px}.nd-wf-summary strong{display:block;margin-top:3px}.nd-wf-field{margin:14px 0}.nd-wf-field label{display:block;font-size:12px;color:#8fb8d8;font-weight:800;text-transform:uppercase;margin-bottom:7px}.nd-wf-field textarea{width:100%;box-sizing:border-box;background:#17374f;border:1px solid #315a7c;color:#eef8ff;border-radius:12px;padding:12px;font-size:16px;min-height:95px;resize:vertical}@media(max-width:600px){#ndWorkflowModal{padding:0}.nd-wf{width:100%;height:100%;max-height:none;border-radius:0;border:0;padding:20px 18px}.nd-wf-clock .time{font-size:52px}}`;document.head.appendChild(s)}
  function modal(){ensureStyle();if(document.getElementById('ndWorkflowModal'))return;const m=document.createElement('div');m.id='ndWorkflowModal';m.innerHTML='<div class="nd-wf" id="ndWorkflowInner"></div>';document.body.appendChild(m)}
  function renderTravel(id){const e=entry(id);if(!e)return;setTime(id,'fahrtStart');const t=getTimes(id);document.getElementById('ndWorkflowInner').innerHTML=`<div class="nd-wf-top"><div><div class="nd-wf-status">🚗 Fahrt läuft</div><h2>${esc(e.titel||'Notdiensteinsatz')}</h2><div class="nd-wf-sub">Zeit läuft seit Fahrtbeginn – reine Dokumentation</div></div><button class="nd-wf-close" onclick="window.closeNdWorkflow()">×</button></div><div class="nd-wf-steps"><div class="nd-wf-step active"></div><div class="nd-wf-step"></div><div class="nd-wf-step"></div></div><div class="nd-wf-clock"><div class="time" id="ndWfTimer">${durationText(duration(t.fahrtStart,now()))}</div><div class="label">Fahrtdauer</div></div><div class="nd-wf-info"><strong>${esc([e.vorname,e.nachname].filter(Boolean).join(' ')||'Kunde')}</strong><span>📍 ${esc(address(e)||'Keine Adresse')}<br>📞 ${esc(e.telefonnummer||'–')}</span></div><button class="nd-wf-btn nd-wf-success" onclick="window.ndArrived('${esc(id)}')">📍 Angekommen – Zeitstempel setzen</button>`;clearInterval(timer);timer=setInterval(()=>{const x=getTimes(id),el=document.getElementById('ndWfTimer');if(el)el.textContent=durationText(duration(x.fahrtStart,now()))},1000)}
  function renderWork(id){const e=entry(id),t=getTimes(id);if(!e)return;clearInterval(timer);document.getElementById('ndWorkflowInner').innerHTML=`<div class="nd-wf-top"><div><div class="nd-wf-status">📍 Vor Ort</div><h2>Einsatz dokumentieren</h2><div class="nd-wf-sub">Was wurde vorgefunden und was wurde durchgeführt?</div></div><button class="nd-wf-close" onclick="window.closeNdWorkflow()">×</button></div><div class="nd-wf-steps"><div class="nd-wf-step done"></div><div class="nd-wf-step active"></div><div class="nd-wf-step"></div></div><div class="nd-wf-summary"><div><small>Fahrt gestartet</small><strong>${fmt(t.fahrtStart)}</strong></div><div><small>Angekommen</small><strong>${fmt(t.angekommen)}</strong></div><div><small>Fahrtdauer</small><strong>${durationText(duration(t.fahrtStart,t.angekommen))}</strong></div><div><small>Arbeitsbeginn</small><strong>${fmt(t.arbeitStart)}</strong></div></div><div class="nd-wf-field"><label>Was wurde vorgefunden?</label><textarea id="ndWfVorgefunden" placeholder="z. B. Schloss defekt, Schlüssel abgebrochen ...">${esc(t.vorgefunden||'')}</textarea></div><div class="nd-wf-field"><label>Was wurde durchgeführt?</label><textarea id="ndWfDurchgefuehrt" placeholder="z. B. Schloss ausgebaut und ersetzt ...">${esc(t.durchgefuehrt||'')}</textarea></div><div class="nd-wf-field"><label>Material / Besonderheiten</label><textarea id="ndWfMaterial" placeholder="Verwendetes Material oder weitere Hinweise ...">${esc(t.materialText||'')}</textarea></div><button class="nd-wf-btn nd-wf-primary" onclick="window.ndContinueToReport('${esc(id)}')">Weiter zum Regiebericht →</button>`}
  window.ndArrived=async function(id){setTime(id,'angekommen');setTime(id,'vor_ort');setTime(id,'arbeitStart');setTime(id,'arbeit');const e=entry(id);if(e)e.status='vor_ort';try{if(window.supabaseReady&&window.supabaseClient&&!String(id).startsWith('LOCAL-')){const r=await window.supabaseClient.from('kalender_eintraege').update({status:'vor_ort'}).eq('id',id);if(r.error)throw r.error}else if(typeof window.saveAppData==='function')window.saveAppData()}catch(err){console.warn('Status Vor Ort konnte nicht gespeichert werden',err)}renderWork(id)};
  window.ndContinueToReport=function(id){const t=getTimes(id);t.vorgefunden=document.getElementById('ndWfVorgefunden')?.value.trim()||'';t.durchgefuehrt=document.getElementById('ndWfDurchgefuehrt')?.value.trim()||'';t.materialText=document.getElementById('ndWfMaterial')?.value.trim()||'';const all=read(KEY,{});all[id]=t;write(KEY,all);activeId=id;if(typeof window.openNotdienstRegiebericht==='function')window.openNotdienstRegiebericht(id);setTimeout(()=>{const el=document.getElementById('rb_bem');if(el){const extra=[t.vorgefunden,t.durchgefuehrt,t.materialText].filter(Boolean).join('\n\n');if(extra)el.value=extra}},100);closeWorkflowOnly()};
  function closeWorkflowOnly(){clearInterval(timer);document.getElementById('ndWorkflowModal')?.classList.remove('open')}window.closeNdWorkflow=closeWorkflowOnly;
  window.startNotdienstWorkflow=async function(id,useNavigation){activeId=id;setTime(id,'fahrtStart');setTime(id,'unterwegs');const e=entry(id);if(e)e.status='unterwegs';if(typeof window.updateNotdienstStatus==='function')await window.updateNotdienstStatus(id,'unterwegs');modal();document.getElementById('ndWorkflowModal').classList.add('open');renderTravel(id);if(useNavigation&&typeof window.openNotdienstNavigation==='function')setTimeout(()=>window.openNotdienstNavigation(id),120)};
  async function waitForNewNotdienst(before,tries=20){for(let i=0;i<tries;i++){const list=(window.AppData?.kalender?.eintraege||[]).filter(e=>e?.typ==='notdienst');const found=list.find(x=>!before.has(String(x.id)));if(found)return found;if(typeof window.loadKalenderFromSupabase==='function'){try{await window.loadKalenderFromSupabase()}catch(e){}}await new Promise(r=>setTimeout(r,250));}return null}
  const originalSave=window.saveNotdienst;window.saveNotdienst=async function(useNavigation){const before=new Set((window.AppData?.kalender?.eintraege||[]).filter(e=>e?.typ==='notdienst').map(e=>String(e.id)));if(typeof originalSave!=='function')return;await originalSave(false);const e=await waitForNewNotdienst(before);if(e)await window.startNotdienstWorkflow(e.id,!!useNavigation);else alert('Der Notfalleinsatz wurde gespeichert, konnte aber nicht geöffnet werden.')};
  function convertButtons(){const form=document.getElementById('notdienstForm');if(!form)return;form.querySelectorAll('button').forEach(b=>{const oc=b.getAttribute('onclick')||'';if(!oc.includes('saveNotdienst'))return;if(oc.includes('true')){b.setAttribute('onclick','saveNotdienst(true)');b.textContent='🚗 Fahrt starten + Navigation'}else{b.setAttribute('onclick','saveNotdienst(false)');b.textContent='🚗 Fahrt starten ohne Navigation'}})}
  const originalGenPDF=window.genPDF;window.genPDF=function(type){const result=typeof originalGenPDF==='function'?originalGenPDF.apply(this,arguments):undefined;if(type==='rb'&&activeId){setTime(activeId,'ende');setTime(activeId,'erledigt');const e=entry(activeId);if(e)e.status='erledigt';if(window.supabaseReady&&window.supabaseClient&&!String(activeId).startsWith('LOCAL-'))window.supabaseClient.from('kalender_eintraege').update({status:'erledigt'}).eq('id',activeId).then(()=>{}).catch(()=>{});if(typeof window.renderNotdienst==='function')window.renderNotdienst()}return result};
  function init(){modal();convertButtons();setInterval(convertButtons,1000)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();